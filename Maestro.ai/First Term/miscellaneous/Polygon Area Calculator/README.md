# Polygon Area Calculator

Small library that provides `Rectangle` and `Square` classes with basic geometry helpers.

Files:
- `polygon_area_calculator.py` — core classes: `Rectangle`, `Square`.
- `polygon_area_calculator.md` — documentation summary.
- `example.py` — simple example runner.

Usage:

```bash
python example.py
```

License: MIT (see LICENSE)
